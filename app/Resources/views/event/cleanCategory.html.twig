{% extends 'base.html.twig' %}

{% block body %}
    <div class="container col-lg-12 text-center">

    <div class="row">
        <div id="accordion" class="col-lg-2 col-md-12">
            <div class="card">
                <div class="card-header">
                    <a class="card-link" data-toggle="collapse" href="#collapseOne">
                        Категории
                    </a>
                </div>
                <div id="collapseOne" class="collapse show" data-parent="#accordion">
                    <div class="card-body">
                        <ul>
                            {% for category in categories %}
                                <li><a href="{{ path('events_category', {'id':category.id}) }}">{{ category.name }}</a></li>
                            {% endfor %}
                            <li><a href="{{ path('game') }}">ClickTrain</a></li>
                        </ul>
                    </div>
                </div>
            </div>
        </div>

        <div class="jumbotron col-lg-9 text-left">
            <h1>Празна категория!</h1>
        </div>

    </div>
{% endblock %}